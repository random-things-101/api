{
  "name": "core-api",
  "version": "1.0.0",
  "description": "REST API for Minecraft Core plugin - manages players, grants, and ranks",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/zod-validator": "^0.2.0",
    "better-sqlite3": "^9.2.2",
    "zod": "^3.22.0",
    "dotenv": "^16.4.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.5.10"
  }
}
